server {
    listen 80 default_server;
    root /home/box/web/index.html;
    location / {
        root /home/box/web/index.html;
    }
    location ^~ /uploads/ \.*$ {
        root /home/box/web;
    }
    location ^* \.*$ {
        alias /home/box/web/public
    }
    location [^.] {
        try_files $uri $uri/ =404;    }
}
